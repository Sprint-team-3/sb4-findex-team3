spring:
  datasource:
    url: jdbc:postgresql://yamanote.proxy.rlwy.net:24280/railway
    username: postgres
    password: aWGmDrHMwruuFHbEhdHPEPIpRnIECaUS
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none # 운영 배포 시엔 none 또는 validate 권장
#    show-sql: true
    show-sql: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
  application:
    name: Findex
  config:
    import: application-secret.yml
  task:
    scheduling:
      enabled: true

batch:
  sync:
    info:
      fixed-delay: P1D
    enabled-index-data:
      fixed-delay: P1D

logging:
  level:
    org.hibernate: INFO
#    org.springframework.data.jpa.domain.support.AuditingEntityListener : DEBUG
#    org.hibernate.SQL: debug
#    org.hibernate.orm.jdbc.bind: trace

external:
  api:
    baseurl: https://apis.data.go.kr/1160100/service/GetMarketIndexInfoService

server:
  forward-headers-strategy: framework
